# Fully-pinned, reproducible environment for scopehound v0.1.0.
#
# These are the EXACT versions the project was developed and tested against
# (runtime + dev tooling + transitive dependencies), captured via `pip freeze`.
# Use this for a reproducible install:
#
#     pip install -r requirements.lock
#     pip install -e . --no-deps        # then install scopehound itself
#
# For a normal install, prefer `pip install -e ".[dev]"`, which resolves the
# flexible version ranges declared in pyproject.toml. Regenerate this lock with:
#
#     pip freeze --exclude-editable > requirements.lock

annotated-doc==0.0.4
colorama==0.4.6
coverage==7.14.1
greenlet==3.5.2
iniconfig==2.3.0
markdown-it-py==4.2.0
mdurl==0.1.2
packaging==26.2
playwright==1.60.0
pluggy==1.6.0
pyee==13.0.1
Pygments==2.20.0
pytest==9.1.1
pytest-cov==7.1.0
PyYAML==6.0.3
rich==15.0.0
ruff==0.15.18
shellingham==1.5.4
typer==0.26.7
typing_extensions==4.15.0
